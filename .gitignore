# Go build artifacts
bin/
dist/
coverage.out
coverage.html

# Local worktrees for parallel agent work
.worktrees/
.claude/worktrees/

# Environment files
.env
.env.local
.env.*.local
*.env
okdctl.env

# Generated terraform tfvars (contains environment-specific values)
infrastructure/terraform/environments/*/terraform.tfvars

# Cluster artifacts
okd-install/
*.kubeconfig
kubeadmin-password

# Terraform files
*.tfstate
*.tfstate.backup
terraform.tfstate.*.bak
*.tfvars
*.tfplan
.terraform/
infrastructure/terraform/modules/**/.terraform.lock.hcl
# Temporary destroy-override files (see modules/proxmox-okd/main.tf lifecycle comment)
override.tf
*_override.tf

# SSH keys and secrets
*.pem
*.key
id_rsa*
*.pub
pull-secret.txt
pull-secret.json
.sops.yaml

automation/config/secrets/*
!automation/config/secrets/.gitkeep
!automation/config/secrets/1password-credentials.json
!automation/config/secrets/1password-token.txt

# Ignition files
*.ign

# Custom ISOs and downloads
*.iso
downloads/
*.tar.gz
*.tgz
*.zip

# Logs and temporary files
*.log
logs/
tmp/
temp/
.tmp/

# Backup files
*.backup
*.backup.*
/etc/haproxy/haproxy.cfg.backup.*

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE/Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Certificate files
*.crt
*.p12
*.pfx
certs/

# CLI generated files
okdctl.yaml
.okdctl/
.okdctl.lock
.deploy-state-*.json

# Test artifacts
test.yaml
demo.yaml


# agent-workflow artifacts — local only, never shipped
.claude/
docs/superpowers/

# local-only maintainer docs — kept on disk, not published
CLAUDE.md
roadmap.md
docs/roadmap/
