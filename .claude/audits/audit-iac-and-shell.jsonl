{"id":"iac:e076e43c:sh-posix-not-bash","skill":"audit-iac-and-shell","cluster":"install-sh-fail-closed","file":"scripts/install.sh","line_start":1,"line_end":1,"loc_touched":1,"smell":"Shebang `#!/bin/sh` constrains the script to POSIX sh (dash on Debian/Ubuntu, ash on Alpine), which prevents unconditional `set -o pipefail`, `[[ ]]`, and other bash conveniences. Script now mitigates with a conditional `(set -o pipefail 2>/dev/null) && set -o pipefail` probe, but future contributors may still introduce bashisms that break silently under dash/ash.","evidence":"#!/bin/sh\n# okdctl installer\n...\nset -eu\n# pipefail is POSIX-optional but supported by bash/ash/dash 0.5.8+.\n# shellcheck disable=SC3040\n(set -o pipefail 2>/dev/null) && set -o pipefail","severity":"suggestion","confidence":"medium","risk":"low","fix_class":"refactor","fix_summary":"Either (a) switch shebang to `#!/usr/bin/env bash` and drop the pipefail probe — bash is available on every supported install target (Debian/Ubuntu/Fedora/RHEL), Alpine is not a target platform per `uname -s` gate; or (b) keep `/bin/sh` and document in a one-line comment above the shebang that POSIX-only constructs are required, so future contributors don't accidentally introduce bashisms. The current hybrid works but sits on a portability knife-edge.","rule_source":["repo-skill: audit-iac-and-shell SKILL.md §3 Agent A","Google Shell Style Guide §Shebang"],"adjacent_linter":"shellcheck:SC2039","adjacent_linter_enabled":true,"scaffolding":false,"seam":"none","claude_conflict":false,"run_date":"2026-04-21"}
{"id":"iac:18a795d5:hcl-no-prevent-destroy-masters","skill":"audit-iac-and-shell","cluster":"hcl-destroy-ordering","file":"infrastructure/terraform/modules/proxmox-okd/main.tf","line_start":140,"line_end":255,"loc_touched":116,"smell":"Master VMs (OKD control plane carrying etcd quorum state) have no `lifecycle { prevent_destroy = true }` guard. A misconfigured `terraform apply` that perturbs a force-new attribute (e.g. `bios`, `machine`, `os_storage`) would destroy and replace every master, losing etcd state. `okdctl destroy` is the intended tear-down path; `terraform apply` should not be able to accidentally destroy the control plane.","evidence":"resource \"proxmox_virtual_environment_vm\" \"master\" {\n  count = length(local.masters)\n  ...\n  lifecycle {\n    precondition {\n      condition     = length(var.master_isos) >= var.master_count\n      error_message = \"master_isos must have at least master_count (${var.master_count}) entries, got ${length(var.master_isos)}.\"\n    }\n    ignore_changes = [network_device, startup, cdrom, boot_order, efi_disk]\n  }\n}","severity":"suggestion","confidence":"medium","risk":"medium","fix_class":"refactor","fix_summary":"Add `prevent_destroy = true` to the master VM resource's `lifecycle` block, gated by a variable (e.g. `var.allow_master_destroy`, default false) that `okdctl destroy` flips before running Terraform. Document in main.tf that operators MUST use `okdctl destroy` to tear down a cluster. Mirrors the `prevent_destroy` pattern on quorum-sensitive resources (RDS instances, etcd PVs). Same rationale applies less strongly to bootstrap (ephemeral post-install) and workers (rebuildable from ignition).","rule_source":["HashiCorp §Lifecycle: prevent_destroy","repo-counter-example: internal/distribution/okd/destroy/*.go (canonical tear-down path)"],"adjacent_linter":"none","adjacent_linter_enabled":false,"scaffolding":false,"seam":"audit-state-and-recovery","claude_conflict":false,"run_date":"2026-04-21"}
{"id":"iac:b803fcb7:ci-no-tflint-tfsec","skill":"audit-iac-and-shell","cluster":"hcl-credential-hygiene","file":".github/workflows/ci.yml","line_start":97,"line_end":109,"loc_touched":13,"smell":"`validate-terraform` + `lint-terraform` jobs now run `terraform fmt`, `terraform validate`, and `tflint -f compact` — but no secret/policy scanner (tfsec, checkov, or trivy config). tflint catches terraform_* idiom issues; tfsec/checkov catch misconfigured provider secrets, missing `sensitive = true`, and public-exposure antipatterns that the HCL surface will grow into as the module adds network/firewall rules.","evidence":"lint-terraform:\n  runs-on: ubuntu-latest\n  steps:\n    - uses: actions/checkout@...\n    - uses: terraform-linters/setup-tflint@...\n    - working-directory: infrastructure/terraform/modules/proxmox-okd\n      run: |\n        tflint --init\n        tflint -f compact","severity":"minor","confidence":"high","risk":"low","fix_class":"config","fix_summary":"Add a `tfsec` or `trivy config` step to the validate-terraform/lint-terraform job. tfsec has a maintained action `aquasecurity/tfsec-action@...`; `trivy config infrastructure/terraform` is a single call. The HCL surface is small today — locking down now keeps the cost low as the module grows to include networking / storage classes / Ceph pools.","rule_source":["CLAUDE.md §tooling","tfsec rule catalog (AVD-*)","checkov policy library"],"adjacent_linter":"tfsec","adjacent_linter_enabled":false,"scaffolding":false,"seam":"none","claude_conflict":false,"run_date":"2026-04-21"}
{"id":"iac:b803fcb7:tflint-no-config","skill":"audit-iac-and-shell","cluster":"hcl-provider-hygiene","file":".github/workflows/ci.yml","line_start":102,"line_end":109,"loc_touched":8,"smell":"CI runs `tflint --init && tflint -f compact` with no `.tflint.hcl` config file in either module or environment directory. Without a config, tflint loads only the default language ruleset — the `terraform-linters/tflint-ruleset-terraform` plugin (recommended preset: module_pinned_source, required_providers, required_version, naming conventions, unused_declarations) is therefore silent.","evidence":"- working-directory: infrastructure/terraform/modules/proxmox-okd\n  run: |\n    tflint --init\n    tflint -f compact\n- working-directory: infrastructure/terraform/environments/production\n  run: |\n    tflint --init\n    tflint -f compact","severity":"suggestion","confidence":"high","risk":"low","fix_class":"config","fix_summary":"Add `infrastructure/terraform/.tflint.hcl` with `plugin \"terraform\" { enabled = true, preset = \"recommended\" }`. Point CI to it via `--config=$GITHUB_WORKSPACE/infrastructure/terraform/.tflint.hcl` (shared across module + env) or per-directory symlinks. The recommended preset catches terraform_unused_declarations, terraform_required_providers, terraform_module_version, terraform_naming_convention — all directly applicable to this module.","rule_source":["tflint docs §Configuring TFLint","terraform-linters/tflint-ruleset-terraform README"],"adjacent_linter":"tflint","adjacent_linter_enabled":true,"scaffolding":false,"seam":"none","claude_conflict":false,"run_date":"2026-04-21"}
