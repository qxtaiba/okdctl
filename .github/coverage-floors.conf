# Per-package coverage floors (percentage, integer or decimal).
# Key is the full import path (e.g. github.com/qxtaiba/okdctl/internal/netutil).
# Key "*"   sets the default floor for any package not listed explicitly.
# Key "total" gates the aggregate total line from go tool cover -func.
#
# All floors start at 0. Raise individual entries as tests land for
# the corresponding package (N12 -> netutil, N13 -> config/validators).
*=0
total=0
github.com/qxtaiba/okdctl/internal/netutil=0
github.com/qxtaiba/okdctl/internal/config=0
